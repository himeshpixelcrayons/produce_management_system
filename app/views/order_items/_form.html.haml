= hidden_field_tag :product_weight, product.try(:weight)
= hidden_field_tag :product_quantity, product.try(:quantity)
= order_item.hidden_field :id, value: order_item.object.id unless order_item.object.new_record?
= order_item.hidden_field :product_id, value: product.try(:id)
%td= product.try(:title)
%td
	= order_item.input :quantity, label: false, input_html: { class: 'quantity', value: order_item.object.quantity }
	%span
%td= order_item.input :weight, label: false
%td= order_item.input :price, input_html: { value: product.try(:price) || order_item.object.price }, label: false
%td= order_item.input :amount, label: false