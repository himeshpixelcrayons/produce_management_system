%h1 Listing Product Types

%table.table.table-bordered.table-striped.table-responsive.table-hover.no-margin
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}

  %tbody
    - @product_types.each do |product_type|
      %tr
        %td= product_type.name
        %td
          = link_to product_type_path(product_type), class: 'btn ink-reaction btn-icon-toggle btn-primary' do
            %i.fa.fa-eye
          = link_to edit_product_type_path(product_type), class: 'btn ink-reaction btn-icon-toggle btn-primary' do
            %i.fa.fa-pencil
          = link_to product_type_path(product_type), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn ink-reaction btn-icon-toggle btn-primary' do
            %i.fa.fa-trash

%br

= link_to 'New Product type', new_product_type_path, class: 'btn ink-reaction btn-accent'
