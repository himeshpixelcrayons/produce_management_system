%h1 Listing Products

%table.table.table-bordered.table-striped.table-responsive.table-hover.no-margin
  %thead
    %tr
      %th Title
      %th Quantity
      %th Price
      %th Product type
      %th Units Of Quantity
      %th{colspan: "3"} Action

  %tbody
    - @products.each do |product|
      %tr
        %td= product.title
        %td= product.quantity
        %td= product.price
        %td= product.try(:product_type).try(:name)
        %td= product.unit_of_quantity
        %td
          = link_to edit_product_path(product), class: 'btn ink-reaction btn-icon-toggle btn-primary' do
            %i.fa.fa-pencil
          = link_to product_path(product), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn ink-reaction btn-icon-toggle btn-primary' do
            %i.fa.fa-trash

%br

= link_to 'New Product', new_product_path, class: 'btn ink-reaction btn-accent-modified'
